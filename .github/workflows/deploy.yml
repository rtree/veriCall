name: Deploy to Cloud Run

on:
  push:
    branches:
      - master
      - main

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  PROJECT_NUMBER: ${{ secrets.GCP_PROJECT_NUMBER }}
  REGION: ${{ secrets.GCP_REGION }}
  SERVICE_NAME: ${{ secrets.GCP_SERVICE_NAME }}
  REPOSITORY: ${{ secrets.GCP_REPOSITORY }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Google Auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      # GitHub Secrets → GCP Secret Manager (個別シークレットとして)
      - name: Sync Secrets to Secret Manager
        run: |
          sync_secret() {
            local name=$1
            local value=$2
            if [ -z "$value" ]; then
              echo "⚠️ $name: 値が空のためスキップ"
              return
            fi
            # 改行を除去して保存
            local clean_value=$(printf '%s' "$value" | tr -d '\n\r')
            if gcloud secrets describe "$name" --project=${{ env.PROJECT_ID }} > /dev/null 2>&1; then
              printf '%s' "$clean_value" | gcloud secrets versions add "$name" --data-file=- --project=${{ env.PROJECT_ID }}
              echo "✅ $name: 更新"
            else
              printf '%s' "$clean_value" | gcloud secrets create "$name" --data-file=- --project=${{ env.PROJECT_ID }}
              echo "✅ $name: 作成"
            fi
          }
          
          echo "🔐 Secret Manager にシークレットを同期中..."
          sync_secret "twilio-account-sid" "${{ secrets.TWILIO_ACCOUNT_SID }}"
          sync_secret "twilio-auth-token" "${{ secrets.TWILIO_AUTH_TOKEN }}"
          sync_secret "twilio-phone-number" "${{ secrets.TWILIO_PHONE_NUMBER }}"
          sync_secret "destination-phone-number" "${{ secrets.DESTINATION_PHONE_NUMBER }}"
          sync_secret "forward-timeout" "${{ secrets.FORWARD_TIMEOUT }}"
          sync_secret "whitelist-numbers" "${{ secrets.WHITELIST_NUMBERS }}"
          sync_secret "sendgrid-api-key" "${{ secrets.SENDGRID_API_KEY }}"
          sync_secret "notification-email" "${{ secrets.NOTIFICATION_EMAIL }}"
          sync_secret "from-email" "${{ secrets.FROM_EMAIL }}"
          echo "✅ シークレット同期完了"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Container
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ github.sha }}
            ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:latest
          cache-from: type=registry,ref=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:buildcache,mode=max

      - name: Deploy to Cloud Run
        id: deploy
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --port 8080 \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 10 \
            --set-env-vars="NODE_ENV=production" \
            --set-secrets="TWILIO_ACCOUNT_SID=twilio-account-sid:latest,TWILIO_AUTH_TOKEN=twilio-auth-token:latest,TWILIO_PHONE_NUMBER=twilio-phone-number:latest,DESTINATION_PHONE_NUMBER=destination-phone-number:latest,FORWARD_TIMEOUT=forward-timeout:latest,WHITELIST_NUMBERS=whitelist-numbers:latest,SENDGRID_API_KEY=sendgrid-api-key:latest,NOTIFICATION_EMAIL=notification-email:latest,FROM_EMAIL=from-email:latest" \
            --service-account ${{ env.PROJECT_NUMBER }}-compute@developer.gserviceaccount.com
          
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region ${{ env.REGION }} --format='value(status.url)')
          echo "url=$URL" >> $GITHUB_OUTPUT
          echo "🚀 Deployed to: $URL"

      - name: Show Output
        run: |
          echo "🚀 Deployment complete!"
          echo "   URL: ${{ steps.deploy.outputs.url }}"
          echo "   Twilio Webhook: ${{ steps.deploy.outputs.url }}/phone/incoming"

      - name: Get Logs on Failure
        if: failure()
        run: |
          echo "=== Fetching Cloud Run logs ==="
          gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=${{ env.SERVICE_NAME }}" \
            --limit 50 \
            --format="table(timestamp, textPayload)" \
            --freshness=10m || echo "Failed to fetch logs"
